# SP1a execution ledger (subagent-driven development, 2026-09-23 → 2026-09-24)

Verbatim copy of the SDD ledger for plan docs/superpowers/plans/2026-09-23-sp1a-kernel-core-store-ipc-cli.md: pre-flight scan, 31 rulings, per-task review outcomes, deferred minors, founder decisions queued.

# SDD ledger — plan: docs/superpowers/plans/2026-09-23-sp1a-kernel-core-store-ipc-cli.md

Spec: docs/superpowers/specs/2026-09-22-verticalai-design.md (reachable; binding authority).
Worktree: .worktrees/sp1-kernel, branch sp1-kernel, base 9da7859 (master).
Build env for every dispatch: `export PATH="$HOME/.cargo/bin:$PATH" CARGO_TARGET_DIR="$HOME/.cargo-target/verticalai-sp1"`; MSVC toolchain; never the GNU toolchain.

## Pre-flight conflict scan (2026-09-23)

| Tasks | Produces vs consumes | Finding |
|---|---|---|
| T0 ↔ T4, T5, T7 | `KernelTestHooks` method set (register_arch, enroll_device, renew_liveness, set_context_budget, approvals_for, hot_modules, infer_log→Vec, stops→StopSet) | identical in all four; stub's `stops()`/`infer_log()` change from borrow to owned — T0 updates vk-props callers. OK |
| T1 ↔ T4, T6, T7 | `Db::TABLES` = arches, registers, tasks, leases, approvals, stops, resumes, liveness, devices, hot, kv_test | every table T4/T6/T7 touch is in the list; approvals keyed `subject:hash`, filtered by prefix in T4. OK |
| T2 ↔ T4, T6 | `BlobStore::put(key_id, label, bytes)`, `get`, `envelope`, `shred`, `is_shredded` | T4 `attach_artefact`/`read_artefact`, T6 ns `/artefacts/<hash>` use exactly these. OK |
| T3 ↔ T4 | `LedgerFs::append(kind, retention, wall_ms, quality, hlc, causal_heads, payload_hash)`, `chain()`, `tail`, `verify` | T4 `log()` and `ledger()` match; T3 adds `Ledger::push_verified` + `LedgerFs::chain()` in vk-contracts/vk-store. OK |
| T4 ↔ T6 | private fields of `RealKernel` (`store`, `stops`, `next_id`, `log`) used by `tasks.rs` | same crate — allowed. `crate::tests::local` reachable from `tasks::tests` if `local` is `pub(crate)` (private module visible to descendants of the crate root). OK |
| T4 ↔ T7 | `RealKernel::devices()`, `arches()`, `store()`, `node_id` pub | T7 lists `devices()` as an addition; `node_id` is pub in T4. OK |
| T6 ↔ T7 | `StepKind` serde (`tag = "kind"`, snake_case) | T7 test JSON `{"kind":"plan","arch_id":…}` matches. OK |
| T7 ↔ T8 | `vk_kernel::presence::KeySource::{Keyring, File(PathBuf)}`, `NodeDevice::load_or_create(src, node_id)` | T8 uses exactly those names; distinct from `vk_store::keys::KeySource`. OK |
| T8 ↔ T9 | `RealKernel::boot()` called by vkd in T8, defined in T9 | **conflict** — see Ruling 1 |
| T4 (self) | RealKernel re-implements `promote`/`export`/`run_automation` bodies verbatim from the stub | rubric "verbatim duplication" — see Ruling 2 |
| T4 (self) | tests: `state_survives_reopen` needs stops/resumes/registers/ledger persisted and reloaded; `i2_and_i4_prime` expects I2 from `infer` with Personal register on a Business-clearance cloud arch | code paths exist: `load()` reloads stops/resumes/devices/arches; `read_register` (caller I2) then `i2_flow` (arch I2). OK |
| T1, T2, T3 (self) | each test asserts observable behaviour (idempotent migrate, encrypted-at-rest, shred survives reopen, partial line recovery, tamper detection) | no empty assertions. OK |
| Global | ledger event kinds used: task.submitted, task.step, register.written, infer, infer.projected, lease.granted, approval.recorded, stop, resume, automation.ran, module.promoted, module.exported, arch.mounted, arch.unmounted, device.enrolled, boot | all in the allowed list. OK |

Ruling 1: T8's `vkd` calls `kernel.boot()` before T9 defines it — T8 adds a minimal `boot()` (verify chain, append `boot`, return `BootReport` with ledger_ok/ledger_len only); T9 extends it (device/arch enumeration, refuse-on-failure unless --force, `vk man`) — because T8 is the CLI milestone and must run on its own — costs one small re-touch of `boot()` in T9 if wrong.
Ruling 2: RealKernel duplicating the stub's `promote`/`export`/`run_automation` bodies is intentional — the stub is the storage-free executable reference the property tests run against alongside the real kernel (plan Task 5); duplication is bounded to those three bodies and interceptors are shared via vk-contracts — costs a two-place edit if those semantics change (spec §3.4/§3.5 are stable).

## Task log
Task 0: dispatched (implementer sonnet/medium, base 9da7859)
Task 0: first implementer stopped by the user mid-task (uncommitted partial work kept); fresh sonnet implementer dispatched to continue from the partial state (user asked to resume)
Task 0: implemented (commit 7780d73, 45/45, clippy+fmt clean); task review dispatched (sonnet) on review-9da7859..7780d73.diff
Task 0: complete (commits 9da7859..7780d73, review clean; controller confirmed cargo test --workspace 45/0)
Task 1: dispatched (implementer sonnet — brief carries complete code; base 7780d73)
Task 1: implemented (commit 17b17e4, 49/49); review dispatched (sonnet)
Task 1: review — Important (plan-mandated): relative --state-dir override bypasses the sync-folder guard (raw string checked before absolutising). Ruling: the spec (§3.8 "refuse a path under a OneDrive/Dropbox/iCloud folder") governs the brief's literal code — fix by absolutising the override against current_dir before the marker check, via a pure `resolve_override(raw, cwd)` so it is testable without changing the process cwd — costs one extra function if wrong.
Task 1: minor (deferred): updated_ms column never populated by put_json (db.rs:57-59)
Task 1: minor (deferred): no explicit test that unknown table names are rejected
Task 1: minor (deferred): TABLES is a module const (db::TABLES), plan prose says Db::TABLES — documentation shorthand, later briefs use the module path
Task 1: ⚠️ resolved: commit trailer present (checked git log); vk-store deps declared in Task 0 (reviewed there); state_dir(None) untested by design
Task 1: fix round 1/5 dispatched-and-returned (commit a82937c, 52/52); scoped re-review (sonnet) on review-17b17e4..a82937c.diff
Task 1: fix round 1/5 (1 addressed, 0 open — relative override absolutised; commits 17b17e4..a82937c)
Task 1: minor (deferred): refuse_sync_folder widened to pub(crate) unnecessarily (paths.rs:50)
Task 1: complete (commits 7780d73..a82937c, review clean after 1 fix round)
Task 2: dispatched (implementer sonnet — complete code in brief; reviewer to be opus; base a82937c)
Task 2: implemented (commit 2e726fb, 55/55). Plan defect surfaced: brief's test binds the store immutably but the sketch declares shred(&mut self); implementer chose Mutex<BTreeSet> interior mutability so shred takes &self. Ruling 3: the plan defect is mine (test binding); either resolution is acceptable if shred semantics (survives reopen, blocks DEK use) hold — the reviewer judges the Mutex on its merits; if it is flagged, the simpler fix is `&mut self` + `let (d, mut s)` in the test — costs one small edit either way.
Task 2: review dispatched (opus — crypto) on review-a82937c..2e726fb.diff
Task 2: review (opus) — Critical 1: cross-subject collision (address = plaintext hash) breaks erasure; Important 2 swallowed remove_file/read_dir errors; 3 dek_for/shred race; 4 world-readable creation + TOCTOU (plan-mandated); 5 unvalidated key_id/hash as path components; 6 put stringifies StorageError (plan-mandated). Minors 7-13 deferred (see below).
Task 2: Ruling 4 (plan defect, Critical 1): the blob address becomes subject-scoped — `hash = hash_bytes(key_id || 0x00 || plaintext)`, documented on BlobEnvelope.hash as the storage address; the brief's `env.hash == hash_bytes(b"hello")` assertion is superseded — keeps get(hash) API, gives every subject its own ciphertext, shred affects only that subject — costs a `content_hash` field later if SP3 needs plaintext identity.
Task 2: Ruling 5 (Important 5): key ids are hex-encoded in filenames (keys/<hex>.dek, shredded/<hex>) — also required because Task 4 uses key ids like `task:<id>` and ':' is illegal in Windows filenames; hashes validated as `sha256:` + 64 lowercase hex before path use — costs nothing later (ids stay opaque strings).
Task 2: minor (deferred): 7 all failures collapse to NotFound (contract lacks Corrupt/Integrity variant); 8 unused Clone on MasterKey; 9 seal/open exported pub (should be pub(crate)); 10 no atomic writes/fsync, shred order DEK-then-tombstone; 11 ciphertext_len includes nonce; 12 fingerprint() byte-slices a String at [..23]; 13 coverage limited to the brief's three tests
Task 2: ⚠️ VK_MASTER_KEY_FILE env wiring: belongs to vkd (Task 8 uses --master-key-file; env var optional) — ledgered for Task 8's dispatch. ⚠️ test tally arithmetic: controller will verify the count after the fix round.
Task 2: fix round 1/5 returned (commit 059658e, 57/57 confirmed by controller); scoped re-review (opus) on review-2e726fb..059658e.diff
Task 2: fix round 1/5 (6 addressed, 0 open — subject-scoped addresses, error propagation, single-guard locking, create_new key file, hex key filenames + hash validation, typed errors; commits 2e726fb..059658e)
Task 2: minor (deferred): 0x00 separator not injective if a key_id contains NUL (blobs.rs:108-111); open() fails on pre-fix raw-name tombstones (no persisted stores exist yet); zero-length master.key after a failed write bricks the store (fails closed); BlobEnvelope.hash doc in vk-contracts does not mention the subject-scoped derivation; .bin/.json written non-atomically; dek_for collapses master-key failures into NotFound
Task 2: complete (commits a82937c..059658e, review clean after 1 fix round)
Task 3: dispatched (implementer sonnet — complete code in brief; reviewer opus; base 059658e)
Task 3: implemented (commit 574cdda, 60/60); review dispatched (opus) on review-059658e..574cdda.diff
Task 3: review (opus) — Critical 1: append after partial-line recovery writes into the unterminated line (event lost, chain bricked); Important 2 any unparseable line anywhere tolerated as "recovery" (tail-truncation attack invisible, plan-mandated); 3 parse error discarded; 4 .flatten() on read_dir drops segments; 5 open/Store::open do not verify the chain (plan-mandated).
Task 3: Ruling 6 (plan defect, C1+I2+I3): tolerance is narrowed to exactly one case — an unparseable LAST line of the HIGHEST segment whose file does not end with '\n'; on open that line is truncated away (set_len to the end of the last complete line) so append is safe; any other unparseable line makes open fail with segment path + line index (corruption/tampering is not recovery) — costs a stricter open for a genuinely rare disk-corruption case, which boot --force can bypass in Task 9.
Task 3: Ruling 7 (I5, plan-mandated): LedgerFs::open and Store::open load without verifying; chain verification is the boot gate (Task 8 minimal boot() verifies; Task 9 adds refuse-unless---force), so `vk ledger verify` and --force diagnostics can still inspect a tampered ledger — the brief's interface prose "verifies the chain" is corrected to a doc comment on both constructors — costs nothing if wrong beyond moving one call.
Task 3: minor (deferred): replay filters any *.jsonl (should match seg-NNNNNN.jsonl); lexicographic sort cliff at segment 1,000,000; inconsistent error context on IO; recovered_partial_line is a pub field; push_verified name overpromises; Store::open does not create state_dir (paths::state_dir does) — undocumented precondition
Task 3: fix round 1/5 returned (commit 0b8e846, 62/62); scoped re-review (sonnet) on review-574cdda..0b8e846.diff
Task 3: fix round 1/5 (5 addressed, 0 open; commits 574cdda..0b8e846)
Task 3: minor (deferred): set_len at ledger_fs.rs:68 lacks path context; read_to_string at :43 lacks path context
Task 3: complete (commits 059658e..0b8e846, review clean after 1 fix round)
Task 4: dispatched (implementer opus — multi-module integration on the invariant path; reviewer opus; base 0b8e846). Rulings 2 (duplication of promote/export/run_automation bodies is intentional) and the Task 2/3 API adaptations carried in the dispatch.
Task 4: implemented (commits 3b63d54 refactor + b82e78e kernel, 65/65); implementer notes: identical ArchIdentity fixtures collide on arch_id (Task 6 tests must vary identity for two live arches); tasks table is written by Task 6. Review dispatched (opus) on review-0b8e846..b82e78e.diff
Task 4: review (opus) — Important 1 approvals_for prefix-matches keys (I1-bearing, diverges from stub); 2 invariant-bearing writes swallowed with let _ (plan-mandated: leases/approvals/stops/resumes/hot/devices/liveness/counter/ledger append); 3 infer event logged only after write_register succeeds; 4 leases written but never replayed (plan-mandated); 5 I4' projection is a flat truncate that drops ROLE/GOAL, tokens_in clamped not measured.
Task 4: Ruling 8 (I2): the brief's `let _ =` on store writes is superseded — add `KernelError::Store(String)` to vk-contracts syscalls; every store failure on a syscall path propagates as Store (NotFound stays for missing objects); `log()` returns Result and is propagated; `stop()` persists before mutating memory; test hooks that return () delegate to new Result-returning kernel methods and `expect` in the hook — costs a contract enum variant (not in the schema registry).
Task 4: Ruling 9 (I4): leases are replayed on open (expired ones dropped) and LockHome fences are persisted in kv under `fence:<resource>` so fences stay monotonic across restarts — costs a few kv rows.
Task 4: Ruling 10 (I5): projection is structural — keep ROLE, GOAL, DECISION lines, then constraints/open questions/evidence in that order until the adapter's count_tokens fits the budget; re-count after projecting; if ROLE+GOAL alone exceed the budget return KernelError::I4Prime; tokens_in is the measured count.
Task 4: Ruling 11 (promoted from minor 14): mount() refuses to replace an already-mounted arch_id whose manifest differs (unmount first) because a same-identity manifest with a different clearance would silently change what i2_flow checks; test fixtures vary the identity's weights hash.
Task 4: minor (deferred): 6 unmount of unknown arch logs arch.unmounted; 7 set_context_budget overrides not persisted; 8 load() recreates persisted arches as MockAdapter silently (SP1b mounts real adapters at boot); 9 raw store error strings forwarded; 10 PROJECTED marker counts bytes; 11 capability not checked against manifest; 12 no boot event on open (Task 8/9); 13 ClockQuality::Synced asserted unconditionally; 14 ArchStats and reopen of approvals/devices/liveness/hot untested, read_artefact refusal untested; 15 local() helper is cfg(test)-only crate-visible
Task 4: fix round 1/5 returned (commit c6598db, 70/70). Ruling 12: three implementer deviations accepted — Db::kv_list_prefix added to vk-store (fence reload needs kv enumeration); load() reads the wall clock for lease expiry (superseding the "now_ms only for mount/unmount/enroll" note); identical re-mount swaps the adapter while keeping manifest/clearance/ledger inert (SP1b must replace mocks with real engines) — costs nothing structural. Scoped re-review (opus) on review-b82e78e..c6598db.diff
Task 4: fix round 1/5 (6 addressed, 1 new Important open — resume() persists before validating the cited stop; commits b82e78e..c6598db)
Task 4: Ruling 13 (promoted minors, same authority paths): for authority-granting syscalls (approve, promote, resume) the ledger event is appended BEFORE the durable mutation so a ledger failure leaves no effect (fail closed); lease() deletes the superseded row on renewal by the same holder so replay restores at most one live row per (resource, partition) — costs nothing structural.
Task 4: minor (deferred): project() is O(units²) in tokenizations; LockTable::acquire decides on first matching row (locks.rs:76-84); LockHome::is_stale has no production caller yet (fencing consumed in SP1b); kv_list_prefix has no direct unit test; approvals_for/hot_modules unwrap_or_default a list error (refusal direction); read_artefact maps blob read failures to NotFound
Task 4: fix round 2/5 returned (commit 8bd07e3, 72/72). Ruling 14: implementer additions accepted — StopSet::has_stop (read-only existence check) added to vk-contracts; lease() scans the small leases table for superseded rows. Scoped re-review (sonnet — small diff) on review-c6598db..8bd07e3.diff
Task 4: fix round 2/5 (3 addressed, 0 open; commits c6598db..8bd07e3). Accepted trade-off (Ruling 13 direction): on a store failure after a successful ledger append, approve/promote/resume leave a false-positive audit event and no effect — detectable, preferred over an unaudited grant.
Task 4: complete (commits 0b8e846..8bd07e3, review clean after 2 fix rounds)
Task 5: dispatched (implementer sonnet — refactor pattern given in brief; reviewer sonnet; base 8bd07e3)
Task 5: implemented (commit 465ed19, 76/76; the dispatch's expected counts 8/75 were the controller's arithmetic error — 9/76 is correct); review dispatched (sonnet)
Task 5: minor (deferred): vk-kernel/vk-store under [dependencies] rather than [dev-dependencies] in vk-props (follows file precedent); `real()` helper duplicated per test file (integration tests are separate crates)
Task 5: complete (commits 8bd07e3..465ed19, review clean)
Task 6: dispatched (implementer opus — sketch must be adapted to Task 4's fix-round internals; reviewer opus; base 465ed19)
Task 6: implemented (commit 85fbdd7, 79/79) with concerns. Ruling 15 (plan gaps surfaced by the implementer): (a) a Failed task is terminal — run_task_step returns it unchanged (retry is a later explicit syscall); (b) a task has ONE id: Task.id == Register.task_id (create_task reads the register minted by submit_task) so blob keys `task:<id>` and the task row agree; (c) `/devices/<id>` resolves to Entry::Device when enrolled; (d) STOP checked once per step (no mid-step pre-emption) is acceptable for the sequential SP1a scheduler. Review dispatched (opus) with these as requirements.
Task 6: review (opus) — Important 1 Failed/Done not terminal (ruling 15a unmet; STOP on a Done task rewrites it); 2 two ids per task (15b unmet); 3 /devices/<id> missing (15c unmet); 4 Release writes plaintext to an unvalidated caller path and the ledger records only an opaque hash (plan-mandated shape); 5 every poll of a waiting task re-logs and re-writes; 6 Release, persisted Stopped/Failed rows and top()-after-reopen untested.
Task 6: Ruling 16 (I4, plan defect): Release destinations are confined to the kernel export root `<state_dir>/exports/` — `to_dir` must be a relative subpath (no absolute paths, no `..`), joined under the root; a distinct ledger event `artefact.released` (added to the allowed kinds) carries task id, artefact hashes and the resolved destination; the CLI's `--release` (Task 8) names a subpath under the export root and `vk task show` prints the resolved path — costs a copy step in the demo (SP1b's demo script copies from the export root).
Task 6: minor (deferred): top() degrades to an empty view on store errors (signature has no error channel); ns::Entry lacks Deserialize (IPC task adds it if needed); `/` lists artefacts but no /artefacts arm; Approve subject = hash_canonical(register) when no artefact is not exposed to humans; production scheduler imports KernelTestHooks for approvals_for; STOP/failure-path save error replaces the original reason
Task 6: fix round 1/5 returned (commit bd95df9, 83/83; also fixed a latent bug — ? inside match arms escaped run_task_step without persisting Failed; step body extracted into run_step). Scoped re-review (opus) on review-85fbdd7..bd95df9.diff
Task 6: fix round 1/5 (6 addressed + latent ?-escape bug fixed, 1 new Important open — ArtefactRef.kind traverses the release filename; commits 85fbdd7..bd95df9)
Task 6: minor (deferred): create_task can leave an orphan register when the label exceeds the caller's clearance (I2 raised after submit_task persisted); zero-step task transitions to Done without a ledger event; polling a stopped task re-writes the row each call; Release with no artefacts logs an empty artefact.released
Task 6: fix round 2/5 returned (commit 00d66cb, 85/85); scoped re-review (sonnet — small diff) on review-bd95df9..00d66cb.diff
Task 6: fix round 2/5 (1 addressed, 0 open; commits bd95df9..00d66cb)
Task 6: minor (deferred): release_file_name does not re-apply the length/non-empty rules to legacy kinds (shape check only)
Task 6: complete (commits 465ed19..00d66cb, review clean after 2 fix rounds)
Task 7: dispatched (implementer fable — principal derivation; reviewer opus; base 00d66cb). Ruling 17: Task 7 owns presence.rs and therefore also adds RealKernel::enroll_node_device(&NodeDevice) (idempotent, `node:<id>`, trust class full) that Task 8's vkd calls; production enrolment goes through enroll_device_persisted (ruling 8), never the test hook.
Task 7: implemented (commit e9c8a44, 96/96). Ruling 18: `device.enroll_node` over the pipe enrols only `node:<node_id>` (idempotent) — a machine principal must not be able to enrol arbitrary human devices (I1); other devices are enrolled by an admin ceremony in SP1b/SP4; dispatch runs on spawn_blocking with the kernel lock held only inside the sync dispatch. Review dispatched (opus) on review-00d66cb..e9c8a44.diff
Task 7: review (opus) — no Critical (I1 at the transport verified). Important 1 accept loop ends serve() on any accept error; 2 approve untested over IPC and forwards a client-controlled Approval.kind; 3 presence proofs on non-ctx methods are ignored without spending the nonce (bearer for 60 s); 4 Unix bind unconditionally removes the socket path and applies 0600 after bind (plan-mandated).
Task 7: Ruling 19 (I2): over the pipe, `approve` accepts only `kind: human` approvals (with presence) — test/audit approvals are produced by in-process gates (SP3), never by a pipe client; other kinds → -32602. Ruling 20 (I3): a `presence` field on ANY method spends the nonce (removed before dispatch) and is rejected with -32602 on methods that do not take a human Ctx — a proof can never outlive the request that carried it; SP1b binds method+params hash into the signed message when passkeys arrive.
Task 7: minor (deferred): default pipe DACL (comment to be softened this round); blanket error codes for arch.mount/unmount; arch mount/unmount take no Ctx (mutation gating later); challenge eviction drops soonest-to-expire; poisoned kernel mutex is permanent and silent; client accepts error responses with any id; test rendezvous via sleep; untested arch.ls/unmount/task.show/task.ls/top/ledger.tail/MAX_LINE/concurrent connections; ed25519-dalek pinned directly in vk-kernel; enroll_node_key duplicates the device-row layout; loose param typing; JSON-RPC id must be u64
Task 7: fix-round-1 implementer stopped by the user (partial transport.rs edit kept); fresh fable implementer dispatched to finish the round (user asked to resume)
Task 7: fix-round-1 implementer lost to rate limit + network outage (only transport.rs partial edit on disk); resume blocked by the auto-mode classifier; fresh fable implementer dispatched (user asked to resume)
Task 7: fix round 1/5 returned (commit 24019f6, 99/99, implementer resumed by the user); scoped re-review (opus) on review-e9c8a44..24019f6.diff
Task 7: fix round 1/5 (5 addressed, 0 open; commits e9c8a44..24019f6)
Task 7: minor (deferred): stale socket removed before parent validation; two-strike Listener rule on Windows; presence on unknown method answers -32602 not -32601; no consecutive-failure ceiling on Unix accept; presence.challenge has no rate limit; bare relative endpoint skips private_dir; test endpoint dirs accumulate in temp
Task 7: complete (commits 00d66cb..24019f6, review clean after 1 fix round)
Task 8: dispatched (implementer opus — multi-crate CLI/daemon integration; reviewer opus; base 24019f6). Ruling 21: the vk-cli smoke test locates vkd next to CARGO_BIN_EXE_vk and runs `cargo build -p vkd` (via env CARGO) if absent — keeps the plan's separate vkd crate. Ruling 1 applies (minimal boot()); Ruling 16 applies (--release names a subpath of the export root; boot.info returns export_root).
Ruling 22: add RPC `task.subject {task_id}` -> `{subject_hash}` backed by `RealKernel::approval_subject(ctx, task_id)` in tasks.rs; the Approve arm calls the same function so the subject rule (last artefact hash, else hash_canonical(register)) lives once — the CLI cannot compute the subject in-process (no register RPC) — cost if wrong: one extra method to retire when SP1b's passkey ceremony carries the subject in its challenge.
Ruling 23 (plan defect, plan lines 1382-1398): the Draft step attaches its raised output (decisions.last() as UTF-8 bytes) as an artefact of kind = the task's artefact_type after infer; Plan and Judge do not attach — without it no task run ever owns an artefact, Release writes nothing and Approve signs a bare register hash, which contradicts the spec's use case (a) (the approved/released object is the proposal) — cost if wrong: a Draft that also attaches when a later arch produces non-text output; SP1b's real arches will revisit the raise path anyway.
Task 8: implementer proceeding assumptions accepted — `vk boot` locates vkd beside vk, spawns it with the state-dir/endpoint/key flags, waits in --foreground, else detaches and polls `vk status`.
Task 8: implementer DONE_WITH_CONCERNS (commit 84e3447, 105/105). Concerns: approve accepted while STOPped (ruling below); Draft now writes a blob + register.written per step (accepted); vkd does not read VK_ENDPOINT (carry to Task 9 README); manual check's `vk boot` left a `vk`/`master` credential in this machine's Credential Manager (report to the founder). Deviations accepted: `vk task ls` dropped as duplicate of `vk ps`; `vk boot` reports "already serving" instead of double-starting.
Ruling 24: `approve` stays accepted while a STOP is in force — STOP (I4) halts autonomy, approvals and resume are human ceremonies gated by I1, and the task still cannot step until resume; both events are ledgered — cost if wrong: a human approval recorded during a STOP that the operator meant as a freeze of everything; revisit when SP1b's scope semantics land.
Task 8: task review (opus) on review-24019f6..84e3447.diff
Task 8: review returned — spec ✅, quality ❌ (4 Important: boot spawn not detached; approve accepted on a task not waiting for approval; vk boot untested and no key-file passthrough; "already serving" ignores state-dir mismatch; 9 minors in task-8-review.md)
Ruling 25: `approval_subject` (hence `task.subject`) refuses unless the task's current step is an Approve step — the Approve arm satisfies this by construction; the CLI surfaces the kernel's message with exit 1 and prints the subject hash on success — a signed approval for a bare register hash on a queued task is a permanent ledger event that can never satisfy the step — cost if wrong: none observed; pre-approval of a future step was never a supported flow.
Ruling 26: `vk boot` gets the same key/endpoint/node-id flags as vkd and forwards them; detached spawn redirects stdio to `<state_dir>/vkd.log` and uses DETACHED_PROCESS|CREATE_NEW_PROCESS_GROUP on Windows and process_group(0) on Unix; `vk boot --json` prints the daemon pid; `boot.info` returns `state_dir` and `vk boot` compares it with the requested dir ("already serving" exit 0 only on match, mismatch is exit 1 naming both dirs); a smoke test covers boot→status→already-serving→mismatch→kill — the Windows service in SP1b supersedes this launcher — cost if wrong: a launcher path SP1b replaces anyway.
Task 8: fix round 1/5 dispatched (implementer resumed, opus)
Task 8: fix round 1/5 returned (commit ea8b43b, 107/107; adds SetHandleInformation to stop the daemon inheriting vk's stdio on Windows); scoped re-review (opus) on review-84e3447..ea8b43b.diff
Task 8: minor (deferred, carry to Task 9): smoke helper builds vkd only when absent, so a stale local vkd.exe is used silently — Ruling 27: always run `cargo build -p vkd` (incremental no-op when fresh); KernelError has no state-conflict variant, so the approve refusal reads "gate verdict missing or failed: …" (SP1b contracts change).
Task 8: fix round 1/5 (6 addressed: I1–I4, F5, F6; 0 open; commits 84e3447..ea8b43b)
Task 8: minor (deferred): F14 a state-dir-mismatch refusal has already created the requested dir; F15 plain `vk boot` after an explicit-dir boot exits 1 (ruling-compliant); F16 two smoke tests call vkd_exe() concurrently → nested cargo builds may race on a clean target (fold into Ruling 27); plus the 9 minors in task-8-review.md
Task 8: complete (commits 84e3447..ea8b43b, review clean after 1 fix round)
Task 9: dispatched (implementer opus — kernel boot semantics + vkd refusal + vk man + README across three crates; reviewer opus; base ea8b43b). Ruling 28: `vk man` embeds the 19 schemas through a hand-written `include_str!` table in crates/vk-cli/src/man.rs (no build.rs, no generated module) with a test that every file under contracts/schemas/ is in the table — the brief allows either; a generated module adds a build step for 19 constants. Ruling 27 applied here: vkd_exe() always runs `cargo build -p vkd` behind a process-wide Once/Mutex.
Task 9: implementer stalled (600 s watchdog) while running the README block on the DEFAULT endpoint; nothing committed; working tree has edits incl. crates/vk-contracts/src/principal.rs (outside the file set). Resumed by the user's instruction with a recovery checklist (stop stray vkd, report machine side effects, justify or revert the contracts edit, finish on a test endpoint with file keys).
Task 9: previous implementer stopped by the founder (won't resume); fresh implementer (opus) dispatched from the uncommitted tree with the recovery checklist. Founder rule (2026-09-23): every major decision passes by the founder — contract types, invariant paths, ledger event set, CLI public surface, merges. Branch sp1-kernel pushed to origin for remote access.
Task 9: implemented (commit 0d31617, 119/119; fresh implementer took over the stopped one's tree; inherited clippy reds fixed). Accepted deviation (Ruling 7 consistent): boot() returns Ok{ledger_ok:false}, the refusal lives in vkd (--force). Inherited contracts accessors kept: DeviceRegistry::ids(), StopSet::stopped_scopes() (non-serde, schema_drift green, contracts/ unchanged).
Task 9: FOUNDER DECISIONS queued (not ruled): (a) should a forced boot on a broken chain leave a `boot.forced` ledger event (event-set change); (b) policies_version placeholder is written as "0" on first boot — keep, or leave absent until SP1b configures policies.
Task 9: minor (deferred): vk boot on a broken chain without --force reports a 10 s timeout instead of the daemon's reason (it is in vkd.log); Git Bash mangles `\.\pipe\` endpoints even with MSYS_NO_PATHCONV=1 (documented in README).
Machine side effect (report to founder): Credential Manager entry `master.vk` from Task 8's manual check still present; removable with `cmdkey /delete:master.vk`.
Task 9: task review (opus) on review-ea8b43b..0d31617.diff
CI: commit 00988d2 widens ci.yml to every branch and adds release.yml (founder asked for costless cloud testing on 2026-09-24; repo is public so Actions is free). Remote Linux bench (sonnet, isolation=remote) launched on sp1-kernel@0d31617.
CI: first 3-OS run on sp1-kernel (00988d2) FAILED at the clippy step on ubuntu and macos (windows cancelled by fail-fast) — Tasks 1–9's Unix-only code paths (unix socket, 0600/0700 perms, process_group) had never been compiled off Windows. Job log needs auth (403); waiting for the remote Linux bench's clippy output to drive the fix.
Task 9: review returned — spec ✅, quality ❌ (2 Important: F1 `vk man` type_of ignores the array form of `type` → nullable fields render as `any`; F2 refused boot reported as a 10 s timeout instead of the ledger refusal — `child.try_wait()` in the poll loop; minors in task-9-review.md incl. devices/policies_version missing from boot.info, README env-var claim now false, no e2e --force test, pre-existing %TEMP% leak from vk-ipc roundtrip tests)
Task 9: fix round 1/5 dispatched (implementer resumed, opus)
CI: step timings — ubuntu clippy failed after 8 s (build-script class), macos clippy failed after 52 s (compile/lint class), windows cancelled at 32 s. Job logs need a GitHub sign-in (403 via API, "Sign in to view logs" in the browser).
Ruling 29 (infra, founder informed): keyring gains the `vendored` feature so Linux builds need no libdbus-1-dev (the 8 s Ubuntu clippy failure is dbus-secret-service → libdbus-sys → pkg-config); runtime unchanged (Secret Service where present, `--master-key-file` on headless Linux); CI matrix gets `fail-fast: false` so each OS reports. Remote Linux fixer (opus, isolation=remote) dispatched on 00988d2 to deliver a verified diff — cost if wrong: a feature flag to revert.
Task 9: fix round 1/5 returned (commit f2036bc, 121/121); scoped re-review (sonnet) on review-00988d2..f2036bc.diff. Implementer concern noted: log_tail on an appended vkd.log may show a previous run's lines above the reason (minor).
Correction: the two `isolation: remote` agents (Linux bench, Unix fixer) ran LOCALLY through the machine's WSL2 Ubuntu (no sudo → rootless gcc/libdbus hacks under ~/vk-bench/rootfs; rustup 1.98.1 installed in WSL). Both stopped by the controller. Machine change: `build-essential` + `pkg-config` installed in WSL Ubuntu as root (no libdbus-1-dev, on purpose, to prove the vendored path). Next: a local implementer runs cargo inside WSL to fix the Unix build, then verifies on Windows, commits in the worktree.
Founder idea (2026-09-24, awaiting his confirmation of specifics): two more arches — Claude via his subscription (Claude Code `claude -p` adapter) and a local Gemma-class model served by an Ollama container (Docker 29.6.1 and WSL2 present; CPU-only on this machine; smaller accurate model for tests). Changes SP1b's arch list (llama-server becomes optional).
Unix fix: leftovers of the stopped agents removed (agent worktree + branch sp1-kernel-unix-fix, WSL ~/vk-bench 352 MB, %TEMP%/vk-linux-bench); their 40-line partial diff saved as unix-fix-partial.diff. WSL Ubuntu now has build-essential + pkg-config (root install) and rustup 1.98.1 (user ericltx, left by the bench). Local implementer (opus) dispatched: builds in WSL against the worktree over /mnt/c with the target dir on the WSL side, verifies on Windows, commits with both trailers, pushes, polls CI.
Task 9: fix round 1/5 (F1, F2 + requested minors addressed, 0 open; commits 0d31617..f2036bc; refusal now reported in 172 ms)
Task 9: minor (deferred): Started::Lost bails without child.kill(); the `any` sweep in man.rs parses only top-level lines (nested branch untested by the sweep); log_tail on an appended vkd.log may show a previous run's lines; F6–F9/F11 in task-9-review.md; pre-existing %TEMP% .tmp* leak from vk-ipc/tests/roundtrip.rs (async server.abort() vs TempDir drop on Windows)
Task 9: complete (commits 0d31617..f2036bc, review clean after 1 fix round)
Unix fix: complete (commit a226059; Linux 125/125, Windows 121/121; CI run 35931390063 success on ubuntu 81 s, macos 91 s, windows 211 s, sign 14 s). Fixes: keyring vendored (Ruling 29, proven without libdbus-1-dev), 4 unwrap_err→let-else (no Debug on key types), macOS ECONNABORTED=53, fail-fast:false, README /tmp/vk-<user>/vk.sock fallback + portable $D.
Unix fix: minor (deferred): macOS sun_path headroom thin (~92–95/104 bytes from test_endpoint(); shorten the uuid if a longer TMPDIR ever breaks it); openssl/openssl-src appear in Cargo.lock as weak deps of `vendored` (never compiled); macOS never reproduced locally (CI is the only macOS oracle).
Final whole-branch review: dispatched (fable — most capable) on review-9da7859..a226059.diff with final-review-context.md (rulings 1–29 + deferred minors).
Final review (fable): With fixes — Critical 1 (no single-writer lock on the state dir; boot appends before the endpoint is bound → double start corrupts the chain and can duplicate ids), Important 6 (ledger in-memory push before disk write; undetected tail truncation; synchronous=NORMAL non-durable vs fsynced ledger; blobs not verified on read / no AAD; Unix state dir + db + ledger + exports world-readable; task views return goal without an I2 check), Minor 16 — see final-review.md.
Ruling 30 (fix wave, none of it changes contracts, the event set or the CLI surface): C1 → exclusive advisory lock file in the state dir taken at Store::open (Windows share_mode(0), Unix flock), endpoint bound before boot() appends; I1 → write+fsync before the in-memory push, failure leaves no phantom; I2 → SQLite kv `ledger.head` {seq, hash} updated after every append, boot compares and reports a shorter ledger as not ok; I3 → PRAGMA synchronous=FULL; I4 → blob address bound as AEAD associated data and re-derived on read, mismatch is an error; I5 → 0700 dirs / 0600 files on Unix for state dir, db (+wal/shm), ledger, exports; I6 → task.show/ls, top and /tasks apply the I2 flow check and hide tasks above the caller's clearance. One fix dispatch (fable), then a scoped re-review (opus). Cost if wrong: on-disk blob format changes before any real store exists.
Final review: fix wave dispatched (fable, base a226059)
Final review: fix wave returned (commit 2e546dc; Windows and Linux suites green; CI run 35935832698 success on all three OSes). Implementer concerns: I1 done as push-then-rollback (Ledger has no pop, contract frozen); boot.info/ledger.verify now report chain AND head; I4 mismatch surfaces as KernelError::Store; blobs written before this commit fail AEAD (no real store exists yet); blob envelope `label` still unauthenticated (deferred → SP1b); stale Unix socket after a refused-after-bind daemon handled by the existing stale check; minors M12, M15 fixed in passing.
Final review: scoped re-review (opus) on review-a226059..2e546dc.diff
Final review: re-review (opus) — all 7 findings closed, no new Critical/Important; Windows 135/135, Linux 141/141; 4 new minors N1–N4 (malformed ledger.head row unreachable by --force / no re-baseline path after a legitimate restore; payload tier files keep umask modes inside the 0700 state dir; visible_to swallows a registers read error as "hidden"; DEK wrap has empty AAD so a .dek renamed to another subject is undetected).
Ruling 31 (residual minors adjudicated, none load-bearing for the SP1a milestone): M1–M11, M13, M14, M16 and N1–N4 are parked as the SP1b backlog with the reviewer's 8 SP1b recommendations (metadata tier honouring the storage design, kernel-issued approval challenges, Ctx on every read surface, lock+head before the Windows service, infer-intent logging + step idempotency keys, zeroize + DEK AAD, keyed blob address, `vk fsck`); M12 and M15 were fixed in the wave. Cost if wrong: SP1b starts with a known backlog rather than a hidden one.
Final review: clean. Workspace preserved as docs/superpowers/reviews/ (final review + ledger), then deleted. Merge into master: FOUNDER DECISION pending.
