# Trusted computing base statement (spec §5)

In scope of the invariants: the kernel daemon, its service account, the OS keyring, the interceptor chain, kernel-launched (governed) inference processes, enrolled device keys.

Outside the TCB, per platform, and surfaced in the inventory as **unmediated channels**:
- any harness that the platform cannot confine (no Job Object/AppContainer, landlock or sandbox-exec available) — it may read the store directly;
- SaaS assistants embedded inside third-party applications;
- ungoverned inference engines not launched by the kernel.

Any object reachable by an unmediated channel is treated as projected to that channel's clearance for I2 purposes; the UI says so.

## Arch adapters (SP1b)

### Claude Code adapter

Pure completion, no tools. The kernel launches the installed `claude` with every built-in tool removed (`--tools ""`), MCP connectors off (`--safe-mode --strict-mcp-config`), one turn (`--max-turns 1`) and no session written to disk (`--no-session-persistence`); the prompt goes in on stdin, so no process list on this machine shows it. The child runs in one fixed, empty directory (`<state_dir>/claude-code-cwd`, `0700` on Unix), which is also the only directory it could reach if a tool were ever re-enabled.

The inference itself is **not governed**: it happens on Anthropic's servers, where no interceptor of ours runs. The manifest says so — `governed: false`, `locality: Cloud`, `jurisdiction: "US"`, `retention_days: 30`, clearance capped at Business with third-party data refused — so I2 refuses to lower anything above that into it. **Egress to Anthropic is not observed in SP1b**: this node records what it sent and what came back, not what the far end did with it.

Subscription use is the founder's own. A claude.ai subscription is a person's, not a product's: nothing is billed per call, so the manifest's `cost_per_1k_tokens_eur` is `0` and the list-price equivalent the CLI reports rides on the `infer` event as `cost_list_usd`, visibly a comparison and not a charge. **Customer nodes use API arches** (task 2b), which carry a key, a per-token price and, for the EU jurisdiction, a different host.

### Ollama adapter

The first **governed** arch, and the only one so far: the kernel starts the container itself (`docker run -d --name vk-ollama -p 127.0.0.1:11434:11434 -v vk-ollama:/root/.ollama --memory 12g --cpus 6 ollama/ollama:0.33.3`), so the inference is a process this node launched, capped at 12 GiB of memory and 6 CPUs, published on loopback only, with the weights in a volume it owns. `governed: true` is claimed only after the caps have been read *back* off the running container (`HostConfig.Memory`, `HostConfig.NanoCpus`): a container running without them is not a governor, whatever it was meant to be started with, and an existing `vk-ollama` whose image or caps are not the ones this mount asks for is refused rather than adopted — the remedy, `vk mount ollama --recreate`, replaces it and keeps the volume. A mount is also refused when a foreign process already answers on the published port while that container is stopped, because the identity would otherwise be read off somebody else's server.

`vk mount ollama --external URL` mounts an Ollama this node did not start — that one is `governed: false`, clearance capped at Business with third-party data refused, and it is an **ungoverned inference engine** in the sense of the list above. `URL` must be loopback (127.0.0.0/8, `::1`, `localhost`): an engine on another host is a register crossing the network under a manifest that still says `locality: Local`, and that arrives with SP4.

Nothing about a governed call leaves the machine: `locality: Local`, `jurisdiction: "local"`, `retention_days: 0`, clearance up to Personal, `cost_per_1k_tokens_eur: 0` and no `cost_list_usd` — a model on this CPU is not billed and has no list price to compare against. That claim is underwritten, not assumed: the adapter's HTTP client is built with `no_proxy`, so `HTTP_PROXY`, `ALL_PROXY` and the platform's own proxy settings cannot route a loopback prompt through a third party. What is **not** claimed: the container is a resource governor, not a sandbox around what the model says. Nothing inspects the completion.

Identity is content-addressed — the arch id hashes the weights' digest from `/api/tags`, the family, parameter size and quantisation, the Ollama version, `num_ctx`, the seed, the digest of the **container image** actually running, and whether the arch is **governed**. The cap *values* are deliberately outside it: the same model under a tighter cap is the same model, only slower, and they are recorded on the mount and on every call's `details` instead. `governed` is inside it because a capped container and an uncapped one carry different clearances (Personal against Business) and different retention, and two manifests that disagree about that must never be able to share an id.

**Its context ceiling is half the window asked for.** Ollama 0.33.3 cuts an oversize prompt to `num_ctx / 2 + 3` tokens and answers HTTP 200 with `done_reason: "stop"` and no error (spike 1a). So the manifest's ceiling is `min(num_ctx, context_length) / 2`; a prompt past nine tenths of that is refused before the call, and an answer whose `prompt_eval_count` reached **that same ceiling** is refused after it — the ceiling, not `num_ctx / 2`, which is the wrong number whenever the model's own window is the binding half. Both are I4′: a confident answer to half a question is the failure this invariant exists to prevent.

## Harness (SP1b, Task 4)

The **harness** is different from the Claude Code *arch*: the arch is a tool-less completion, the harness is Claude Code run as an agent *with* tools, confined, for a `StepKind::Harness` step. It reaches the kernel only through one stdio MCP server (`vk-mcp`), launched under a lease whose id is the token every `harness.*` syscall carries; the kernel maps that token to a machine principal at the harness clearance (up to Business, third-party data refused) and answers as if the harness itself had called. An unknown or expired token is refused (I1 → `E_INVARIANT`), and no `harness.*` method accepts a presence proof — a lease is a machine capability, not a human's presence.

**SP1b harness: filesystem and process isolation via workspace ACL + Job Object; network egress of the harness is observed (netwatch) not blocked; restricted-token launch: feasible on this machine but deferred (see below).**

- **Filesystem.** The harness sees a label-projected workspace, `<state_dir>/harness/<task_id>/` (`0700` on Unix), not the store: only the register's goal (`TASK.md`), its decisions (`PLAN.md`), and the evidence artefacts whose label *flows to* the harness clearance (`BRIEF/`) are written into it — each projection recorded on the ledger (rides `infer.projected`, no new event kind), each artefact above the clearance refused. Its writes land in `OUT/`, which the kernel attaches and then removes with the workspace (unless `--keep`).
- **Process.** The tree is launched under the Job Object governor (`JobGovernor::new(2 GiB, None)`): it dies when the kernel's handle closes and is capped at 2 GiB. `claude.exe` is launched directly, never a `.cmd` shim, and contained immediately after spawn; `governed: true` is claimed only on a verified `IsProcessInJob` read-back (Ruling 18). A Job Object kill leaves the child's exit status `0`, so the harness records `killed by governor` / `timeout` from what it did, not from the code.
- **Network egress is observed, not blocked.** `netwatch::sample` reads the launched pid's established TCP connections (`GetExtendedTcpTable`, IPv4 and IPv6) every 500 ms into the run record, appended to the ledger as one `harness.connections` event per run (founder decision 2026-09-24). It is telemetry, not a firewall — a confined network namespace is later-sprint work — and it is keyed by the *owning pid*, so a connection a helper process opens on the harness's behalf is missed; Claude Code's native binary owns its own TLS sockets. What egress is *allowed* is Anthropic's servers; the run record is what lets an auditor check that.
- **Restricted-token launch (spike 4a(iii), 2026-09-24): feasible on this machine, deferred to a hardening pass.** From an ordinary interactive session (privileges: only `SeShutdown`, `SeChangeNotify`, `SeUndock`, `SeIncreaseWorkingSet`, `SeTimeZone` — no `SeAssignPrimaryToken`, `SeIncreaseQuota` or `SeImpersonate`), `CreateRestrictedToken(DISABLE_MAX_PRIVILEGE)` on the process's own token followed by **`CreateProcessAsUserW` succeeded** and ran a restricted child — the "restricted token of the caller's own token" special case bypasses the assign-primary-token privilege. `CreateProcessWithTokenW` failed with `ERROR_PRIVILEGE_NOT_HELD` (no `SeImpersonate`). So the mechanism exists here; SP1b nonetheless ships **Job Object + workspace ACL + netwatch** as the confinement, and restricted-token launch is the **open TCB item** — because (a) a *meaningful* restriction (restricting SIDs / low integrity, not just dropping privileges) has to be shown to leave the 240 MB Claude Code binary able to run, do TLS and write its cwd, (b) the daemon's real deployment is under a service/virtual account (Task 6), whose privileges differ and must be re-validated, and (c) it is a founder checkpoint. Until then the harness that a platform cannot *meaningfully* restrict remains an **unmediated channel** in the sense above: the Job Object caps and kills it, and the workspace ACL and netwatch bound and observe it, but nothing yet stops a compromised harness from reading what its own token can read.
